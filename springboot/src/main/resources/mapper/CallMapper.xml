<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aix.againhello.call.mapper.CallMapper">

    <insert id="insertDeceasedData" parameterType="com.aix.againhello.common.DeceasedDataDTO" useGeneratedKeys="true" keyProperty="deceasedCode">
        INSERT INTO deceased_data (
            deceased_name,
            gender,
            deceased_age,
            personality,
            deceased_nickname,
            user_nickname,
            relationship,
            speaking_tone,
            tone_style,
            common_phrases,
            example_lines
        ) VALUES (
                     #{deceasedName},
                     #{gender},
                     #{deceasedAge},
                     #{personality},
                     #{deceasedNickname},
                     #{userNickname},
                     #{relationship},
                     #{speakingTone},
                     #{toneStyle},
                     #{commonPhrases},
                     #{exampleLines}
                 )
    </insert>

    <insert id="insertRawFile" parameterType="com.aix.againhello.common.RawFileDTO" useGeneratedKeys="true" keyProperty="code">
        INSERT INTO raw_file (
            subscription_code,
            audio_file_paths
        ) VALUES (
                     #{subscriptionCode},
                     #{audioFilePaths}
                 )
    </insert>

    <update id="updateDeceasedData" parameterType="com.aix.againhello.common.DeceasedDataDTO">
        UPDATE deceased_data
        <set>
            <if test="deceasedName != null">
                deceased_name = #{deceasedName},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="deceasedAge != null">
                deceased_age = #{deceasedAge},
            </if>
            <if test="personality != null">
                personality = #{personality},
            </if>
            <if test="deceasedNickname != null">
                deceased_nickname = #{deceasedNickname},
            </if>
            <if test="userNickname != null">
                user_nickname = #{userNickname},
            </if>
            <if test="relationship != null">
                relationship = #{relationship},
            </if>
            <if test="speakingTone != null">
                speaking_tone = #{speakingTone},
            </if>
            <if test="toneStyle != null">
                tone_style = #{toneStyle},
            </if>
            <if test="commonPhrases != null">
                common_phrases = #{commonPhrases},
            </if>
            <if test="exampleLines != null">
                example_lines = #{exampleLines}
            </if>
        </set>
        WHERE deceased_code = #{deceasedCode}
    </update>

</mapper>
