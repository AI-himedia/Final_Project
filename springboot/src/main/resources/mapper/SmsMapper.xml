<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0.4//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aix.againhello.sms.SmsMapper">

    <!--  고인 데이터 추가 테스트  -->
    <insert id="insertDeceasedData" parameterType="com.aix.againhello.sms.DeceasedDataDTO">
        INSERT INTO deceased_data (name, gender, age, personality, deceased_nickname, user_nickname, relationship, speaking_tone)
        VALUES (
        #{name},
        #{gender},
        #{age},
        #{personality},
        #{deceasedNickname},
        #{userNickname},
        #{relationship},
        #{speakingTone}
        )
    </insert>

</mapper>