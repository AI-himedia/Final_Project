# def save_all_to_db(subscription_code: int, deceased_info: dict, chat_url: str):
    # deceased_code = insert_deceased_data(deceased_info)
    # update_subscription_with_deceased(subscription_code, deceased_code)
    # insert_raw_file(subscription_code, chat_url)
